#include "main.h"
/**
 * _abs - Computing absolute vale of an integer
 * @c: Computed absolute number
 * Return: Absolute value
 */
int  _abs(int c)
{
	if (c < 0)
	{
		int _abs_val;

		abs_val = c * -1;
		return  (abs_val);
	}
	return (c);
}
