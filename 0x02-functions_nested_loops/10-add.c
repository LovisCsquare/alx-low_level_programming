#include "main.h"
/**
 * add - function that adds two integers and returns result10-add.c
 * @a: First integer a
 * @b: Second integer b
 * Return: Sum of two integers a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
