#include <stdio.h>
/**
 * main - Letters of the alphabets
 * Return: 0
 */
int main(void)
{i
	char letter;
	2-print_alphabet.c
	for (letter = 'a'; letter <= 'z'; letter++)

		putchar(letter);

	putchar('\n');

	return (0);
}
